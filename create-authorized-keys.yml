---
  - name: Create passwordless login for all accounts on all nodes
    hosts: duvar
    remote_user: ec2-user 
    become: yes
    tasks:
    - name: Create authorized_keys file for each account
      shell:
         chdir: /home/{{ item }}/.ssh
         cmd: cat id_ed25519.pub >> authorized_keys 
      loop:
         - talhao
         - numanm
         - cuneytk
         - caglayans
         - ranak
         - huseyink
         - yilmazd
         - turguty
